# Tasks

Active execution queue only.
Backlog や仕様差分は `docs/tasks.md` を使う。

## TODO

- [ ] `docs/tasks.md` を「今月の焦点」と「履歴 / 将来拡張」に整理し、毎回読むコンテキスト量を下げる
- [ ] 変更種別ごとの必須 skill / verify ルールを明文化し、UI 変更では `visual-qa` と token 利用確認を必須化する
- [ ] `lint + tsc + test + build` を自動で回す CI 導線を追加し、ローカル運用依存を減らす

## In Progress
