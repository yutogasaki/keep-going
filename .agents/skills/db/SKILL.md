# /db - データベーススキーマ確認

引数でテーブル名が指定された場合はそのテーブルのみ、指定なしなら全テーブルを対象にする。

## 手順

1. `supabase/deploy.sql` を読み込む
2. 以下の情報を抽出して表示:
   - テーブル一覧（引数があればフィルタ）
   - 各テーブルのカラム名・型・制約
   - RLSポリシー
   - インデックス
3. 簡潔なテーブル形式で出力する
