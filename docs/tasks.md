# KeepGoing — Product Backlog / Spec Gap

> このファイルは product backlog と仕様差分の整理用。
> Active execution queue は `.agents/tasks/TASKS.md` を使う。

> セッション間で進捗を共有するために `docs/` に保管。
> 4仕様書（Core v1.4 / UI v7 / Logic v2.0 / Operation v1.0）と照合済み。
> 最終更新: 2026-03-08

---

## 2026-03 ブラッシュアップ・ロードマップ

今月は「子どもに楽しい」「保護者に安全」を壊さず、まず信頼性と重い箇所を整える。

| 優先 | 項目 | 狙い | 注意点 |
|------|------|------|--------|
| P0 | Sync 競合解決 / pull restore | データ喪失と不信感を防ぐ | 正常系だけでなく既存データと競合時 UX まで決める |
| P0 | persist / migrate 安全性 | 状態追加時の事故を防ぐ | `types/createState/migrate/test` を常にセットで更新 |
| P0 | `menuGroups` / `customGroups` 循環依存解消 | build warning を実害化させない | import の責務を整理しないと再発する |
| P0 | effect warning 解消 | 見えにくい UI バグを減らす | `useEffect` を黙らせるだけの修正は禁止 |
| P1 | 巨大ファイル分割 | 修正精度と開発速度を上げる | 先に責務を切らないと分割だけで終わる |
| P1 | セッション再開 / 戻り先の一貫性 | 「終わったあとどうなるか」を明確にする | FAB 方式と仕様差分を整理してから決める |
| P1 | 記録データ強化 | 保護者と先生に説明しやすくする | 内部ログを増やしすぎると実装が重くなる |
| P1 | 先生 / 家族 UX の意味明確化 | 誤操作を減らす | 可愛さより明示性を優先する |
| P2 | アクセシビリティとレスポンシブ | 長期運用の事故を減らす | modal / focus / aria を横断で見る必要がある |
| P2 | TTS / 音量 / 演出 polish | 継続体験を上げる | 過剰演出で圧を出さない |

### 今月やる 10 項目

- [x] pullAndMerge のコンフリクト処理実装
- [x] クラウド pull / 新端末 restore の仕様確定と実装
- [x] `menuGroups` / `customGroups` の循環依存解消
- [x] React Hook dependency warning 解消
- [x] TeacherMenuEditor / TeacherExerciseEditor / SingleExerciseEditor の共通シェル化
- [x] `useMenuPageData` 分割
- [x] MenuSettingsSection のロジック分離
- [x] 記録データに種目別回数とスキップ情報を追加
- [x] セッション終了後の戻り先と同日再開ルールを整理
- [x] アクセシビリティ基礎改善（editor modal, GroupCard, focus, aria）
- [x] TTS / 音量設定の MVP を決める

### いまは後ろに置くもの

- [ ] 先生メール管理の `user_roles` 移行
- [x] `initialSync` の逐次 await を並列化
- [x] `fetchAllStudents` のページネーション
- [ ] BGM の本格導入
- [ ] Figma 前提のデザイン運用

---

## 2026-03 開発運用 backlog

プロダクト改善を継続しつつ、開発効率・精度・運用しやすさを落とさないための backlog。
active execution queue へ移すのは `.agents/tasks/TASKS.md` に限定する。

### 今すぐやる 5 項目

| 状況 | 優先 | 項目 | 狙い | 注意点 |
|------|------|------|------|--------|
| [x] | P0 | `AGENTS.md` / `CLAUDE.md` canonical 化 | guide の重複更新と context 消費を減らす | 参照先を増やしすぎず、入口は短く保つ |
| [x] | P0 | `.agents/tasks/DONE.md` 月次圧縮 | 履歴を要約ログへ戻し、探索コストを下げる | 大きな設計判断は消さずに残す |
| [ ] | P1 | `docs/tasks.md` 軽量化 | 毎回読む backlog のコンテキスト量を下げる | archive を増やしすぎて single source を壊さない |
| [ ] | P1 | skill / verify の必須化 | persist / UI / governance 変更の漏れを減らす | 儀式化しすぎて手数だけ増やさない |
| [ ] | P1 | CI で `lint + tsc + test + build` 実行 | ローカル運用依存を減らし、精度を安定させる | 失敗時の修復コストが高い flaky check は避ける |

### まだやらなくてよいもの

- [ ] E2E / visual snapshot の常設化
- [ ] coverage threshold の厳格化
- [ ] Figma 前提のデザイン運用
- [ ] ADR / release runbook / rollback 手順の整備
- [ ] design token 直書き色の全画面一括掃除

### 補足メモ

- UI 変更では desktop / mobile の visual QA を先に運用へ定着させ、その後に自動化を検討する
- design token の徹底は、一括置換より変更頻度の高い画面から段階的に進める
- security / privacy / observability は別軸で継続確認するが、今月の最優先は governance と verification の安定化

---

## ✅ 完了

### インフラ・セットアップ
- [x] Vite + React + TypeScript
- [x] Zustand（persist middleware付き）
- [x] Tailwind CSS v4 + Framer Motion
- [x] Google Fonts（Outfit / Noto Sans JP / JetBrains Mono）
- [x] localforage 永続化基盤

### デザインシステム
- [x] カラーパレット（ピーチ→ミントグラデ + ティールアクセント）
- [x] グラスモーフィズムカードスタイル
- [x] BalletShoeIcon カスタムSVG

### ナビゲーション
- [x] 5タブ構成（ホーム / きろく / 🩰FAB / メニュー / せってい）
- [x] タブ切り替え動作

### 種目データ
- [x] 仕様書準拠の全11種目マスター（`src/data/exercises.ts`）
- [x] クラスレベル別フィルタリング関数

### Home画面（ユーザー決定: FAB方式）
- [x] TODAYダッシュボード + 中央FABでストレッチ開始
- [x] 今日の進捗リング（localforageから動的読込）

### オンボーディング（仕様§10）
- [x] Welcome画面
- [x] クラス選択 4択 → Zustand永続化（仕様§10.4）
- [x] スワイプチュートリアル（仕様§10.3）

### きろく画面（ユーザー決定: 数値OK）
- [x] 合計回数/分/日数のスタッツカード
- [x] 日別セッション履歴リスト
- [x] 空状態表示

---

## 🟡 実装済み（要改善・仕様との差分あり）

### ストレッチセッション（Home = TikTok型再生画面）
**基本動作:**
- [x] 5カウントダウン → 自動再生（仕様§0.6）
- [x] タイマーリング + 残り秒数表示（仕様§5.1）
- [x] タップで一時停止/再開（仕様§3.1）
- [x] 停止中の暗転 + ▶アイコン（仕様§5.2）
- [x] 上スワイプで次へ（仕様§3.1）
- [x] 下スワイプで前へ + 端バウンス（仕様§3.5）
- [x] スワイプヒント「↑ 次へ」常時表示（仕様§3.6）
- [x] 自動遷移 3秒プレビュー（仕様§6.2）
- [x] カウント中の操作（上スワイプ=スキップ, タップ=停止）（仕様§3.2）
- [x] 自動遷移中の操作（上スワイプ=即開始, タップ=停止）（仕様§3.3）
- [x] 左右切替「はんたいがわへ」通知（R30→L30 種目）
- [x] バックグラウンド復帰 → 先頭から5カウント再開（仕様§3.8）
- [x] 無限フロー = ループ（仕様§0.5）

**区切り:**
- [x] 大きな区切り ≒15分 →「続ける」ボタン、終了がデフォルト（仕様§7.2）
- [x] 小さな区切り ≒5分 → 短い余白で自然に続く（仕様§7.1）

**クラス連動:**
- [x] セッション生成がクラスレベル連動

**未実装:**
- [x] 時間パッキング最適化（同一部位の連続回避、体幹は後半配置）（Logic §6）
- [ ] タブ移動時の再生停止 + 戻った時に最初から5カウント（仕様§3.7）
  - ※ FAB方式ではセッション=オーバーレイのためタブ移動不可 → 仕様不要の可能性
- [ ] 同日再起動時に前回ストレッチの最初から再開（仕様§4.1）
  - ※ 現在はFAB方式のためセッションは毎回新規生成

### メニュー画面 — ✅ メニューグループ・単品カスタマイズ実装済み
- [x] **プリセットグループ 5種**（基本ストレッチ/開脚じゅうてん/体幹つよくなる/バレエのまえに/ぜんぶやる!）
- [x] **カスタムグループ作成**（絵文字/名前/種目マルチ選択/繰り返しOK）
- [x] **カスタムグループ編集・削除**
- [x] **単品メニューのカスタマイズと作成機能** (NEW)
- [x] **グループ展開で中身プレビュー**（種目タグ表示）
- [x] **グループタップ → 指定種目でセッション開始**
- [x] **S10 Y字バランス追加**（左右種目、全クラス）
- [x] クラスレベル連動フィルター
- [x] メニューから選んだ種目終了後、パッシブ体験に戻る（仕様§6.4）

### せってい画面（仕様§11.4）
- [x] クラス選択（4択）→ Zustand永続化
- [x] データリセット + 確認ダイアログ
- [x] アプリ情報
- [x] **アカウントセクション**（`src/pages/settings/AccountSection.tsx`）
  - ログイン済み：メール表示 / 同期状態（同期中スピナー or「クラウド同期 ON」）/ ログアウトボタン
  - 未ログイン：ログイン/アカウント作成ボタン
- [x] 音量設定（仕様§11.4）
- [x] 通知設定（仕様§11.4）

---

## ❌ 未実装

### 種目選定ロジック（Logic §5）
- [x] **候補抽出**: クラス適合 + 種別適合 + blacklist除外
- [x] **Daily Must注入**: High優先度を先に選ぶ仕組みで実質対応済み
- [x] **時間パッキング**: warmup→main→core順、emoji連続回避、30/60秒単位
- [x] **スキップ置換**: 当日セッション間は除外。1セッション内2回出現時の2回目は許容（実害なし）

### ⚙️ せってい画面の拡充（優先度: 中）
- [x] アプリ内の効果音・音声ガイダンスの「音量調節」スライダー。
- [x] 通知設定（「毎日忘れないようにリマインドする」トグル等、将来のPWA見据え）。

### 記録・データ仕様（Logic §7 — 部分的）
- [x] 実施日、合計実行時間 → 保存済み
- [x] 実施種目ID → 保存済み
- [x] **種目ごとの実行回数** → `sessionRecords` helper で保存/読込時に正規化（Logic §7.1）
- [x] **スキップ情報（内部参照）** → `skippedIds` を正規化しつつ内部参照で保持（Logic §7.1）
- [x] **記録は完全自動 + ユーザー修正不可** → 保存/読込/集計を同一 helper に統一

### バックエンド・クラウド
- [x] **Supabase クライアントセットアップ**（`src/lib/supabase.ts`）
- [x] **Supabase DBスキーマ定義**（`supabase/schema.sql`）
  - family_members / sessions / custom_exercises / menu_groups / app_settings テーブル
  - RLS（Row Level Security）ポリシー設定済み
  - updated_at 自動更新トリガー
- [x] **認証**（`src/contexts/AuthContext.tsx`）
  - メールアドレス＋パスワード / Google OAuth
  - サインイン/サインアップ/サインアウト
  - ログイン時に自動で初回同期 + 毎分キュー処理
- [x] **ログイン画面**（`src/pages/LoginPage.tsx`）
  - Google でログイン / メール＋パスワード
  - 新規登録時はメール確認フロー
- [x] **クラウド同期（Push側）**（`src/lib/sync.ts`）
  - オフラインキュー（localforage）＋オンライン復帰時の自動処理
  - 初回ログイン時にローカルデータを全件アップロード（initialSync）
  - 対象：セッション / 家族メンバー / カスタム種目 / メニューグループ / アプリ設定
- [x] クラウドからのデータ取得（新端末リストア・Pull側）
- [x] Vercelデプロイ
- [x] PWA対応

### オンボーディング追加ステップ
- [x] アカウント作成（スキップ可能）（仕様§10.5）— SettingsページのAccountSectionから遷移
- [ ] オンボーディングフロー内でのアカウント作成ステップ（仕様§10.5 正式実装）
- [ ] 通知許可（仕様§10.6）

---

## ⏸ 将来拡張（Operation §4 — 今は実装しない）

### 継続時の選択肢拡張（Operation §1）
- 「続ける」時に「体幹中心で続ける」「ストレッチ中心で続ける」選択肢
- 判断回数制約との整合性が未検討 → 先送り

### 育成（ぴよ）機能（Operation §2）
- たまご → ひよこ → 大人の変化UI
- 「ぴよ」タブで確認
- コア体験の完成を優先 → 先送り

---

## 🔑 次に取り組む課題（優先順）

1. ~~Home画面の方針~~ → **FAB方式で決定** ✅
2. ~~きろく画面の数値~~ → **緩和して維持** ✅
3. ~~セッション仕様準拠~~ → **大幅実装済み** ✅
4. ~~クラスレベル連動~~ → **実装済み** ✅
5. ~~オンボーディング~~ → **3ステップ実装済み** ✅
6. **God-in-Detail UX Polish** (進行中)
   - [ ] 🎵 **BGM実装**: 素材準備でき次第。
   - [x] 🗣️ **TTS自然化**: iOS Enhanced voice 優先選択を実装済み。
7. ~~**種目選定ロジック強化**~~ → 調査の結果ほぼ実装済みと判明 ✅
8. **メニュー「組み合わせ」対応**: カテゴリビュー + 選んだ種目の優先実行フロー
9. ~~**記録データ強化**~~ → 実装済み ✅
10. **レスポンシブ対応**

---

## 技術スタック
| 分類 | 技術 |
|------|------|
| フレームワーク | Vite + React 19 + TypeScript |
| 状態管理 | Zustand (persist) |
| スタイル | Tailwind CSS v4 + インラインスタイル |
| アニメーション | Framer Motion |
| アイコン | Lucide React + カスタムSVG |
| ローカルDB | localforage |
| フォント | Outfit / Noto Sans JP / JetBrains Mono |
| バックエンド | Supabase（認証・クラウド同期）|

---

*最終更新: 2026-03-08*
