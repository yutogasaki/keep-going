# ADR Index

強い設計判断を残すための入口。

この repo では、次のような判断を ADR 候補として扱う。

- Sync / restore / migration の後方互換に関わる判断
- challenge engine の共通化など、複数画面に効く設計判断
- 長期的に参照する module boundary や data contract

まだ ADR 本文がない間は、この index を導線として使う。
判断を正式に残す時は `docs/adr/YYYY-MM-<topic>.md` で追加する。
