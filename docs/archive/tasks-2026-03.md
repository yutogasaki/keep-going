# KeepGoing — Backlog Snapshot Archive (2026-03)

このファイルは `docs/tasks.md` から切り出した履歴参照用 snapshot。
current backlog / spec gap の正本は引き続き `docs/tasks.md`。

> 抽出日: 2026-03-08
> 目的: `docs/tasks.md` を「今月の焦点」と未完了 backlog に絞るため

---

## 2026-03 で閉じた主要項目

- [x] pullAndMerge のコンフリクト処理実装
- [x] クラウド pull / 新端末 restore の仕様確定と実装
- [x] `menuGroups` / `customGroups` の循環依存解消
- [x] React Hook dependency warning 解消
- [x] TeacherMenuEditor / TeacherExerciseEditor / SingleExerciseEditor の共通シェル化
- [x] `useMenuPageData` 分割
- [x] MenuSettingsSection のロジック分離
- [x] 記録データに種目別回数とスキップ情報を追加
- [x] セッション終了後の戻り先と同日再開ルールを整理
- [x] アクセシビリティ基礎改善（editor modal, GroupCard, focus, aria）
- [x] TTS / 音量設定の MVP を決める

## 完了済みの土台

### インフラ・セットアップ

- Vite + React + TypeScript
- Zustand（persist middleware付き）
- Tailwind CSS v4 + Framer Motion
- Google Fonts（Outfit / Noto Sans JP / JetBrains Mono）
- localforage 永続化基盤
- Supabase クライアント / 認証 / Push / Pull / PWA / Vercel デプロイ

### デザインシステム

- ピーチ→ミントのカラーパレット
- グラスモーフィズムカード
- BalletShoeIcon カスタム SVG
- 日本語 UI 文言のハードコード運用

### 主要導線

- 5 タブ構成
- Home の FAB 方式
- きろく画面の stat / 履歴
- オンボーディング 3 ステップ
- メニューグループ / 種目カスタマイズ
- せってい画面のクラス / データ / アカウント / 音量 / 通知

## 実装済みだが仕様差分や改善余地が残るもの

### セッション

- TikTok 型の再生画面、カウントダウン、自動再生、スワイプ、区切り、TTS は実装済み
- 同一部位の連続回避や体幹後半配置を含む時間パッキング最適化は実装済み
- 仕様差分として、FAB overlay 前提のため `タブ移動時の再生停止` は不要化の可能性がある

### メニュー / 記録 / 設定

- メニューからの指定種目セッションとパッシブホーム復帰は実装済み
- 記録は自動保存・集計 helper 統一まで実装済み
- 通知設定 UI はあるが、通知許可フローは未実装

## 2026-03-08 時点で未完了だった項目の snapshot

- 先生メール管理の `user_roles` 移行
- オンボーディング内のアカウント作成ステップ正式実装
- 通知許可
- メニュー「組み合わせ」対応
- レスポンシブ対応
- BGM の本格導入
- Figma 前提のデザイン運用

## 当時の優先順メモ

1. God-in-Detail UX Polish
2. メニュー「組み合わせ」対応
3. レスポンシブ対応

## 技術スタック snapshot

| 分類 | 技術 |
|------|------|
| フレームワーク | Vite + React 19 + TypeScript |
| 状態管理 | Zustand (persist) |
| スタイル | Tailwind CSS v4 + インラインスタイル |
| アニメーション | Framer Motion |
| ローカルDB | localforage |
| バックエンド | Supabase |

---

履歴参照が増えたら、この archive を月次 or テーマ単位で追加する。

